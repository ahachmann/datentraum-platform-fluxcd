apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - open-cost.yaml
configMapGenerator:
  - name: open-cost-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true

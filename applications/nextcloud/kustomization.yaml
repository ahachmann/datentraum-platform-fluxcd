apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: vanille-shop-nextcloud
resources:
  - secrets.yaml
#  - volumes.yaml
  - nextcloud.yaml
configMapGenerator:
  - name: nextcloud-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true


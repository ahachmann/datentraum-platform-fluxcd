apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: collabora
resources:
#  - secrets.yaml
#  - volumes.yaml
  - collabora.yaml
configMapGenerator:
  - name: collabora-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true


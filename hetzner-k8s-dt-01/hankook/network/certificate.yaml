---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hankook-tls-cert
  namespace: hankook
spec:
  secretName: hankook-tls-secret
  dnsNames:
    - hankook.hachmann.hamburg
  issuerRef:
    name: letsencrypt-staging-dns01
    kind: ClusterIssuer